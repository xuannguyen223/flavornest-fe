import axiosInstance from './axiosInstance';

// auth service functions: login, logout, register
// dưới đây là ví dụ cách dùng, không cần phải viêt lại axiosInstance trong file này
// tham khảo cách viết

// export const loginService = async (email: string, password: string) => {
//     const response = await axiosInstance.post("/auth/login", { email, password }, { withCredentials: true });
//     return response.data;
// };
